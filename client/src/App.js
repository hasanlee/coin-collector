import { Route, Routes } from "react-router-dom";

function App() {
  return (
    <div className='h-full bg-slate-100 dark:bg-slate-900 duration-300'>
      <Routes>{/* <Route path='/' exact element={<MainPage />} /> */}</Routes>
    </div>
  );
}

export default App;
